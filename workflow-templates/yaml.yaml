name: YAML
on:
  push:
    branches:
      - $default-branch
    paths:
      - "**.yml"
      - "**.yaml"
  pull_request:
    paths:
      - "**.yml"
      - "**.yaml"
  workflow_dispatch:
jobs:
  lint:
    uses: parcelLab/.github/.github/workflows/yaml.yaml@v1
